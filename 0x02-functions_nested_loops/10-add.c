#include "main.h"
/**
 * int add - function that adds two integers and returns the result
 * @x: the first num
 * @y: the second num
 * Return: the addition of the numbers
 */
int add(int x, int y)
{
	return (a + b);
}
